[provider]
name = "ollama"
aliases = ["ollama-cloud", "local"]
binary = "qwen"
install_package = "@qwen-code/qwen-code"

[provider.config_dir]
path = ".qwen"
format = "json"
filename = "settings.json"
mcp_key = "mcpServers"

[provider.prompt]
flag = "-p"

[provider.system_prompt]
source_file = "PROMPT.md"

[provider.danger]
flag = "--dangerously-skip-permissions"

[provider.model]
flag = "--model"
default = "qwen3.5:397b-cloud"

[provider.env_overrides]
HOME = "/opt/codex-home"
OPENAI_BASE_URL = "http://host.docker.internal:11434/v1"
OPENAI_API_KEY = "ollama"
OPENAI_MODEL = "qwen3.5:397b-cloud"

[provider.validation]
flags = ["--model", "-p"]
danger_flags = ["--dangerously-skip-permissions"]

[provider.login]
command = """set -euo pipefail; export PATH="/usr/local/share/npm-global/bin:${PATH}"; echo "[nemesis8] ollama provider — run 'ollama signin' on the host if not already authenticated."; qwen"""
