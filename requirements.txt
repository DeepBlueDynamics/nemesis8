# Core MCP runtime
aiohttp
httpx
anthropic
openai
fastmcp
# Official MCP SDK — hyperia-mcp.py is now a stdio->streamable-HTTP proxy that
# imports mcp.client.streamable_http / mcp.client.session directly, so declare
# it explicitly (a recent version is required for the streamable-HTTP client)
# rather than relying on fastmcp to pull it in transitively.
mcp
tomlkit
requests
twilio

# Google APIs
google-auth>=2.48.1,<3.0.0
rsa>=4.9,<5
google-auth-oauthlib
google-auth-httplib2
google-api-python-client
google-ads>=26.0.0
python-dotenv

pydantic

# Audio / transcription
faster-whisper>=1.0.3
av>=12,<13
elevenlabs

# Fuzzy matching utilities
rapidfuzz>=3.0

# Gemini API SDK
google-generativeai

# OpenSearch client
opensearch-py

# Dog bark detection (YAMNet-based MCP)
tensorflow
librosa
numpy

# File watching utilities
watchdog

# PDF / document parsing
pypdf
pdfplumber
pymupdf

# Image processing
Pillow

# Simon Willison's `llm` CLI — the universal contract for agent prompting
# (paste-image becomes `llm -a <path>`, tools via `llm -T`, etc).
llm
llm-anthropic
llm-gemini

# DOCX parsing
python-docx
docx2txt

# XLSX parsing
openpyxl

# Hugging Face CLI (hf papers read <arxiv_id>, model/dataset downloads, etc.)
huggingface_hub[cli]
