# Core MCP runtime
aiohttp
httpx
anthropic
openai
fastmcp
# Official MCP SDK — hyperia-mcp.py is now a stdio->streamable-HTTP proxy that
# imports mcp.client.streamable_http / mcp.client.session directly, so declare
# it explicitly (a recent version is required for the streamable-HTTP client)
# rather than relying on fastmcp to pull it in transitively.
mcp
tomlkit
requests
twilio

# Google APIs (Calendar / Drive / Gmail / Gemini SDK)
google-auth>=2.48.1,<3.0.0
rsa>=4.9,<5
google-auth-oauthlib
google-auth-httplib2
google-api-python-client
# NOTE: google-ads REMOVED from the base — it's a heavy, niche dep used only by
# the gads MCP tool (Google Ads management). To use that tool, install it in the
# container on demand:  pip install --break-system-packages google-ads
python-dotenv

pydantic

# Audio
# NOTE: speech-to-text (faster-whisper + av) was REMOVED from the base image —
# transcription runs as a separate sidecar (services/transcription) and the
# transcribe-wav MCP tool is a thin HTTP client to it. Saved ~1-2 GB.
elevenlabs

# Fuzzy matching utilities
rapidfuzz>=3.0

# Gemini API SDKs: the legacy one (google.generativeai, used by google-gemini.py)
# and the new unified GenAI SDK (from google import genai, used by genai_image.py).
google-generativeai
google-genai

# OpenSearch client
opensearch-py

# Numeric core (used across MCP tools)
numpy
# NOTE: tensorflow + librosa were REMOVED — they were pulled in ONLY by the
# disabled MCP/disabled/dog-bark-detector.py (YAMNet). That dropped ~2-3 GB.
# Ambient-sound classification now lives in the transcription sidecar's
# /classify endpoint; re-enable the tool as an HTTP client, or pip-install
# tensorflow+librosa on demand if you truly need it in-container.

# File watching utilities
watchdog

# PDF / document parsing (pdfplumber removed — no active tool imports it;
# pypdf + pymupdf cover the PDF MCP tools)
pypdf
pymupdf

# Image processing
Pillow

# Simon Willison's `llm` CLI — the universal contract for agent prompting
# (paste-image becomes `llm -a <path>`, tools via `llm -T`, etc).
llm
llm-anthropic
llm-gemini

# DOCX parsing
python-docx
docx2txt

# XLSX parsing
openpyxl

# (huggingface_hub removed — no active tool imports it and nothing invokes the
#  `hf` CLI; install on demand if you need it.)
